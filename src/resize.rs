use image::{FilterType, GenericImage, Pixel};
use std::fs::File;

pub mod resize {
    pub fn resize(args: &[String]) -> Result<(), &'static str> {
        // check if enough params are passed
        Ok(())
    }
 
}